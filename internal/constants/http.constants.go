package constants

// Change this if used by other url
const OTHER_SERVICE = "https://localhost:8080"

const (
	ROOT     = "/"
	WEBHOOKS = "webhooks"
)

const (
	WEBHOOKS_PATH = ROOT + WEBHOOKS + ROOT
)
